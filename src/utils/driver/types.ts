export type CreateDriverParams = {

    user_id: number;

    driver_name: string;
    
    driver_contact: string;
}

export type UpdateDriverParams = {
    user_id: number;

    driver_name: string;
    
    driver_contact: string;
}
